Freshness policy:
- Freshness checks run in parallel and must not block the initial answer.
- If a newer edition is suspected or confirmed, keep the answer but mark the stale source explicitly.
- State which version was used locally and which newer version appears to exist remotely when known.
